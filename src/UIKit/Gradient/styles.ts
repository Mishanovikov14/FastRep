import { StyleSheet } from 'react-native';

export const getStyles = () => {
  const styles = StyleSheet.create({
    gradient: {
      height: 319,
      left: 0,
      position: 'absolute',
      right: 0,
      top: 0,
    },
  });

  return styles;
};
